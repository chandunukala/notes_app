Replace this file with the two required screenshots before submission:
1. ui-preview.png — browser view showing at least two notes.
2. delete-action.png — browser view after deletion with DevTools Network tab showing DELETE /api/notes/:id returning 200 OK.
